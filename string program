#include<stdio.h>
#include<string.h>

int main()
{
    char str[40], str2[30];
    puts("enter any string: ");
    gets(str);
    puts("enter any string2: ");
        gets(str2);

    strcat(str, str2);
    puts(str);
    return 0;
}
